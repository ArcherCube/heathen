{
  "name": "@heathen/console",
  "private": true,
  "version": "1.1.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "lint": "eslint --ext .ts,.tsx  --max-warnings 0 \"./\" ",
    "tsc": "tsc -p ./tsconfig.json --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "pnpm build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dayjs": "^1.11.13"
  }
}
